<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.michaeldonally.realityquestv3">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".playerCreationActivity" />
        <activity android:name=".TravelActivity" />
        <activity android:name=".Event_Activity" />
        <activity android:name=".Event_Activity2" />
        <activity android:name=".QuestEnd" />
        <application android:name=".GameData" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBf3zC-ptrAAi7LlvIicITqbN6vgT_Lm6U"/>
        <meta-data
            android:name="com.google.android.maps.v2.AIzaSyBf3zC-ptrAAi7LlvIicITqbN6vgT_Lm6U"
            android:value="AIzaSyBf3zC-ptrAAi7LlvIicITqbN6vgT_Lm6U" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".MainActivity"
            android:label="Reality Quest">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Event_Activity" />
        <activity android:name=".Event_Activity2" />
        <activity android:name=".Event_Activity3" />
        <activity android:name=".QuestEnd"></activity>
    </application>

</manifest>